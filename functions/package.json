{
  "name": "fittrack-functions",
  "version": "0.1.0",
  "description": "FitTrack backend — Firebase Cloud Functions (AI proxy, targets math, food/exercise data)",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions,firestore,auth,storage",
    "shell": "npm run build && firebase functions:shell",
    "deploy": "npm run build && firebase deploy --only functions",
    "seed": "npm run build && node lib/seed/run.js",
    "lint": "eslint --ext .ts src",
    "test": "node --test lib/**/*.test.js"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  },
  "private": true
}
