# Node / Cloud Functions
node_modules/
functions/node_modules/
functions/lib/
functions/.env
functions/.env.*
*.local

# Firebase
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log

# Secrets / service accounts — never commit
*serviceAccount*.json
GoogleService-Info.plist
.env

# Xcode / Swift
ios/**/xcuserdata/
ios/**/*.xcuserstate
ios/.build/
ios/DerivedData/
ios/build/

# Python venv for the App Store Connect scripts (PyJWT + cryptography).
# Recreate with: cd ios && uv venv .venv && uv pip install --python .venv/bin/python PyJWT cryptography
ios/.venv/
.DS_Store
